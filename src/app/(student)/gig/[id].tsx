import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { useLocalSearchParams } from 'expo-router';
import { useState } from 'react';
import { Alert, ScrollView, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Button, colors, Field } from '@/components/ui';
import { StatusTracker } from '@/components/status-tracker';
import { apiErrorMessage } from '@/config/api';
import { applyToGig, getGig, startGig, submitGig } from '@/lib/gig-api';
import { useAuth } from '@/providers/auth-provider';

export default function StudentGigScreen() { const { id } = useLocalSearchParams<{ id: string }>(); const { token, user } = useAuth(); const client = useQueryClient(); const [proposal, setProposal] = useState(''); const [experience, setExperience] = useState(''); const [availability, setAvailability] = useState(''); const [fileUrl, setFileUrl] = useState(''); const [note, setNote] = useState(''); const query = useQuery({ queryKey: ['gig', id], queryFn: () => getGig(token!, id), enabled: !!token && !!id }); const refresh = () => { client.invalidateQueries({ queryKey: ['gig', id] }); client.invalidateQueries({ queryKey: ['my-gigs'] }); client.invalidateQueries({ queryKey: ['gigs'] }); }; const apply = useMutation({ mutationFn: () => applyToGig(token!, id, { proposal, relevantExperience: experience, availability }), onSuccess: refresh, onError: (e) => Alert.alert('Could not apply', apiErrorMessage(e)) }); const start = useMutation({ mutationFn: () => startGig(token!, id), onSuccess: refresh, onError: (e) => Alert.alert('Could not start', apiErrorMessage(e)) }); const submit = useMutation({ mutationFn: () => submitGig(token!, id, { fileUrl, note }), onSuccess: refresh, onError: (e) => Alert.alert('Could not submit', apiErrorMessage(e)) }); const gig = query.data; const application = gig?.applications?.find((item) => item.studentId === user?.id); if (!gig) return null; return <SafeAreaView style={s.safe}><ScrollView contentContainerStyle={s.content}><Text style={s.heading}>{gig.title}</Text><Text style={s.budget}>₹{Number(gig.budget).toLocaleString()}</Text><StatusTracker status={gig.status} /><Text style={s.copy}>{gig.description}</Text><Text style={s.meta}>Due {new Date(gig.deadline).toLocaleDateString()} · {gig.location}</Text><View style={s.chips}>{gig.skillsRequired.map((item) => <Text style={s.chip} key={item}>{item}</Text>)}</View>{!application && gig.status === 'OPEN' && <View style={s.card}><Text style={s.section}>Apply for this gig</Text><Field label="Proposal" value={proposal} onChangeText={setProposal} multiline /><Field label="Relevant experience" value={experience} onChangeText={setExperience} multiline /><Field label="Availability" value={availability} onChangeText={setAvailability} placeholder="e.g. Evenings this week" /><Button title={apply.isPending ? 'Applying…' : 'Apply'} disabled={apply.isPending} onPress={() => apply.mutate()} /></View>}{application && <View style={s.card}><Text style={s.section}>Your application: {application.status}</Text>{gig.status === 'ASSIGNED' && <Button title="Start work" onPress={() => start.mutate()} disabled={start.isPending} />}{(gig.status === 'IN_PROGRESS' || gig.status === 'REVISION_REQUESTED') && <><Field label="Deliverable URL" value={fileUrl} onChangeText={setFileUrl} placeholder="Upload via /api/upload, then paste its URL" /><Field label="Submission note" value={note} onChangeText={setNote} multiline /><Button title={submit.isPending ? 'Submitting…' : 'Submit deliverable'} disabled={!fileUrl || submit.isPending} onPress={() => submit.mutate()} /></>}{gig.revisionRequests?.[0] && <Text style={s.revision}>Latest feedback: {gig.revisionRequests[0].feedback}</Text>}</View>}</ScrollView></SafeAreaView>; }
const s = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.pale }, content: { padding: 20, gap: 15 }, heading: { fontSize: 28, color: colors.navy, fontWeight: '800' }, budget: { color: colors.blue, fontWeight: '800', fontSize: 20 }, copy: { color: colors.text, lineHeight: 22 }, meta: { color: colors.muted }, chips: { flexDirection: 'row', flexWrap: 'wrap', gap: 6 }, chip: { color: colors.blue, backgroundColor: colors.white, borderRadius: 8, padding: 7 }, card: { backgroundColor: colors.white, padding: 16, borderRadius: 14, gap: 13 }, section: { color: colors.navy, fontSize: 18, fontWeight: '800' }, revision: { color: colors.danger } });
