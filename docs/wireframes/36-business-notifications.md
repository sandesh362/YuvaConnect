# 36 · Business Notifications

Route: shares `/(shared)/notifications` with 21 (business type set).

## 1. What the wireframe shows

- Header: title1 "Notifications" + GEAR icon right (not double-check).
- Check-mark rail: "✓ All" + outlined "Applicants" "Work" "Payments".
- Section captions (NOT full-bleed bars here): "New Activity" "Earlier Today" "Yesterday".
- **New Activity rows on washed slate-blue cards** (radius 12, spaced): SOLID circle + bold
  title + right caption time + 2-line body: blue "New Applicant" 2m · green "Deliverable
  Submitted" 15m.
- **Earlier Today rows = plain (no wash), hairline-separated**: blue "Payment Successful"
  3h · amber "Work Update" 5h · green "Verification Complete" 8h.
- Tab bar BUSINESS: Home · Gigs · Post · **Alerts (ACTIVE filled bell)** · Profile.

## 2. Mapping = 21. Note the two row treatments (new = washed card, older = plain rows) —
   replicate exactly.
## 3. Flags: gear (settings target absent) → flag/hide.
## 4–5. At build time.
